---
type: 操作文档
摘要: 2026-09-04 上板作战卡——三条线一页流程（串口↔Aurora 内环 / UDP 网口 / UDP+SFP 内环混合），位流路径、参数、判据、发我清单
created: 2026-09-04
updated: 2026-09-04
related: "[[操作文档/挂起/阶段二之四_串口Aurora上下位机验证实操单]] · [[操作文档/阶段二之五_以太网UDP光口环回实操单]] · [[操作文档/挂起/阶段二之六_UDP_SFP内环混合实操单]]"
---

# 上板作战卡 · 2026-09-04

> **今日目标**：三条线判据一次收完（对应三张实操单）。**分工**：烧录/接线/观测=你；判读/迭代=我。
> **开工前一次性准备**：① JTAG（FT2232H-A）接好；② 网线 PC↔GE1；③ PC 网卡静态 IP `192.168.1.102/255.255.255.0`；④ 网口调试助手 + Wireshark 就绪；⑤ 串口 = **COM7**（FT2232H-B，注册表 SERIALCOMM 实证；COM3/4 是蓝牙虚拟口勿用）。

---

## 线 1 · 串口 ↔ Aurora 内环（二之四实操单 §5）⏸️ 挂起 09-04

> **挂起状态**：✅ 已证链路层完全健康（新位流 ILA：channel_up=1 / lane_up=1 / 零误码——09-02 桥位流的 Aurora 部分正常，桥出复位）。⏸️ 剩两个待办：① dbg 计数器探针在 Set Up Debug 中时钟域显示 undefined（dbg_frame_cnt_i 等，疑 generate 分支内时钟域传播断链）——恢复时先解；② AE33 输入方向判别（M1 只证过 AF34 出向）。恢复入口 = 解完 ① 后按判读表三分支。

**位流**：`D:\FPGA\aurora_64b66b_loop_ex\aurora_64b66b_0_ex\aurora_64b66b_0_ex.runs\impl_1\aurora_64b66b_0_exdes.bit`（HW Manager 烧录）

| 步 | 操作 | PASS 判据 |
|---|---|---|
| 1 | 串口终端（MobaXterm）开 **COM7 @ 115200 8N1 无流控** | 口选对：打字有响应（哪怕乱码） |
| 2 | **L1 冒烟**：打字 | 原样回显；乱码=发我 |
| 3 | **L2 半严格**：`python D:\FPGA\uart_sfp\l3_loopback.py COM7 115200 65536`（64KB 随机） | PASS |
| 4 | **L3 压测**：`python D:\FPGA\uart_sfp\l3_loopback.py COM7 115200 1000000`（1MB，~90s） | PASS |
| 5 | 提速复测：同脚本 `921600` | PASS（加分，失败不阻塞） |

**发我**：L3 脚本输出 + （若失败）rx_fail.bin 首错位 + ILA 截图（channel_up / 误码计数）。

## 线 2 · UDP 网口栈（二之五实操单 §7）

**位流**：`D:\FPGA\project_6\prj\project_6.runs\impl_1\eth_udp_loop.bit`

| 步 | 操作 | PASS 判据 |
|---|---|---|
| 1 | 烧位流 → 等 3s | PC 网卡"正在识别"→"**未识别的网络**" |
| 2 | `ping 192.168.1.10`（加分） | 有应答 |
| 3 | 网口调试助手：本地 UDP `192.168.1.102:1234`，远程 `192.168.1.10:1234`，发文本 | 接收区**逐字节一致** |
| 4 | Wireshark 抓"以太网" → 再点发送 | 四包链：ARP 请求/应答 + UDP 往返；回包源 .10 → 目的 .102 |

**发我**：Wireshark 四包链截图（L3 过）或卡住在哪一级。

## 线 3 · UDP + SFP 内环混合（二之六实操单 §2-4）

**位流**：`D:\FPGA\project_7\prj\project_7.runs\impl_1\sfp_udp_inner_loop.bit`

| 步 | 操作 | PASS 判据 |
|---|---|---|
| 1 | 烧位流 → 等 3s | PC 网卡"未识别的网络"（同线 2 步 1） |
| 2 | 网口调试助手发文本（同线 2 步 3） | **接收区有回显**（回显路径不受 TAP 影响） |
| 3 | **看 T22 灯** | 发送后 1~2s **粘滞点亮** = ⭐ 核心判据：UDP 帧穿 SFP 前端内环并被解析 |
| 4 | （进阶）Set Up Debug → 触发 `dbg_rx2_done` ↑ | ILA 看回环帧：前导码 `55×7+D5` 开头、目的 MAC=PC、载荷=发送文本；泵计数 wr=rd、drop=0 |

**发我**：T22 状态 + ILA 计数器截图（dbg_pump_wr/rd/drop）；不亮则我按预案改 configuration_vector（每轮 10min）。

---

## 顺序建议与备份预案

- **推荐顺序**：线 2（最简，先验证 PC 工具链）→ 线 3（同一套工具，多看两个灯）→ 线 1（换串口终端）。
- **卡住原则**：单卡 15min 无进展 → 截图发我切下一条，别死磕。
- 每条线完成立即在对应实操单勾选 + 截图归档（串口 `D:\FPGA\uart_sfp\docs\`、UDP `project_6\docs\`、内环 `project_7\docs\`）。
