# 毕设 FPGA 工程进度总结（2026-08-11）

## 一、里程碑状态

🎉 **M1 达成（2026-08-11 晚）**：Vitis 导入硬件平台 → Hello World 串口打印成功（COM7@9600）。
阶段一"软硬件协同流程（MicroBlaze 软核 + Vitis）"核心目标完成。

## 二、硬件与工具链基线（实测修正后）

- **板卡**：Kintex UltraScale XCKU060，part = **`xcku060-ffva1156-2-i`（非 CIV 变体）**，FFVA1156 封装、速度等级 -2、工业级；无硬核 ARM → MicroBlaze 软核方案
- **板载晶振**：100MHz 差分（官方 KU_IO.xdc `create_clock -period 10.000`；clk_p=AK17，DIFF_HSTL_I_12）
- **板载 SPI Flash**：N25Q128（128Mb=16MB），出厂镜像可正常启动；MODE 引脚焊死 001（SPI），无拨码/跳线可改
- **UART**：板载 CH340（PC 端 COM7@9600；端口号随插拔漂移，以设备管理器为准）
- **JTAG**：正点原子 FT2232H 下载线（Vivado 识别为 Digilent JTAG-HS1），驱动 Digilent Adept Runtime 2.18.2；同一时刻只允许一个客户端占线
- **工具链**：Vivado/Vitis **2023.1**（D:\Xilinx）；License ENTERPRISE 至 2026-10-05
- **关键引脚**（官方 KU_IO.xdc）：clk_p=AK17（N 自动配对）、reset=AC34（低有效）、uart_rxd=AE33 / uart_txd=AF34、key[0]=Y30、led[0]=T22、led[1]=T23（均 LVCMOS18）

## 三、工程现状

- `D:\FPGA\project_1`：Vivado 2023.1 GUI 工程（**当前事实源**）
  - BD design_1：MicroBlaze 最小系统 = Local Memory 64KB + AXI UART Lite（**9600**）+ AXI Interconnect + MDM + clk_wiz（100MHz 差分输入）+ rst_clk_wiz_1_100M
  - 约束：`ku060_pins.xdc`（已注册进 constrs_1，纯英文注释防 GBK 乱码；无手动 create_clock，防 18-1055 覆盖警告）
  - 综合 0 错误；实现 0 时序违例（WNS +15.2ns）；`design_1_wrapper.bit` + `.xsa` 已导出
- `D:\FPGA\test\led`：LED 冒烟测试工程（按键取反→LED，无时钟无 IP；曾用于定位问题，无保留价值）
- `D:\FPGA\test\project_1`：旧工程（2026.1 时代、CIV part，**已废弃，勿用其任何产物**）
- `D:\FPGA\GPT建议.md`：外部 AI 诊断文档（UG570/UG908/UG912 引用，排查过程留档）
- ⚠️ `D:\FPGA` 目前**不是 git 仓库**（vivado_project/ 删除时随目录丢失）——待 git init

## 四、M1 最终工作流（= 以后的标准流程）

1. Vivado：设计 → 综合/实现 → Generate Bitstream → File → Export Hardware（含 bitstream）→ `.xsa`
2. Hardware Manager：Program Device 烧录 bitstream（成功标志 `End of startup status: HIGH`）
3. Vitis：导入/更新 `.xsa` 硬件规格 → Run Configuration **取消 Program FPGA 勾选**（保留 Reset entire system）→ Run（走 MDM 下载程序到 BRAM）→ 串口 9600 见 Hello World

## 五、调试复盘（答辩素材）

**现象**：Vitis Program FPGA 报 `DONE PIN is not HIGH`（2023.1）/ `End of startup status: LOW`（2026.1）。

**排查链**：
1. 假设①：MODE 引脚焊死 SPI(001) 阻碍 JTAG 配置 → 外部 AI 引 UG570 反驳（JTAG 配置与 MODE 引脚选择无关）；且 Flash 启动成功证明 startup/DONE 硬件良好
2. 假设②：JTAG 链路不稳（FT_Write=0、偶发成功）→ LED 冒烟测试在 HW Manager 下稳定成功，链路排除
3. **关键对照**：LED 位流（part=`xcku060-ffva1156-2-i`）烧录成功 vs MicroBlaze 位流（part=`xcku060_CIV-ffva1156-2-i`）失败 → 位流头部 part 字段对比 → **根因 = part 选错**（CIV 系学长教学视频参数，本板实物为非 CIV）
4. 修正 part → 重跑综合/实现 → 烧录成功 → M1 达成

**结论**：硬件参数以实测为准，教程/视频只能当参考；对照实验 + 位流元数据是定位此类问题的利器。

## 六、遗留与风险

- git 版本追踪未建立（红线 3，待办）
- Flash 烧录方案未实施（当前无需：JTAG 路径已验证可用；若将来需要脱离电脑演示再评估，注意 N25Q128=16MB 与 .bit 24MB 的容量差异，需实测 write_cfgmem 产物体积）
- `test/` 旧工程建议清理或标注废弃，防误用
- 串口 COM 号随插拔漂移

## 七、下一步

1. `git init` 重立版本追踪（红线 3）
2. **阶段二：AXI 总线族**——以 design_1 的 AXI Interconnect + UART Lite（0x40600000）地址映射为活教材
3. 可选：`write_bd_tcl` 把 design_1.bd 固化成脚本防工程丢失
